export interface CountryCode{
    alpha2Code: string;
}

export interface WeatherOutput{
    temp: number,
    temp_min: number,
    temp_max: number,
    pressure: number,
    sea_level: number,
    grnd_level: number,
    humidity: number
}

